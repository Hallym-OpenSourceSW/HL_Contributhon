def absVal(num)
    """
    Function to fins absolute value of numbers.
    >>absVal(-5)
    5
    >>absVal(0)
    0
    """
    if Num < 0:
        return -num
    else:
        return num

print(absVal(-34)) # = 34
